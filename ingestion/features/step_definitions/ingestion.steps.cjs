const { Given, Then } = require('@cucumber/cucumber');

Given('the ingestion Lambda stub is deployed', function () {
  // stub — nothing to arrange yet
});

Then('it accepts no real work yet', function () {
  // stub — nothing to assert yet
});
